{
  "name": "@bridge-to-fig/plugin",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.130.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.0"
  }
}
